<%= form_with(model: curse, local: true) do |form| %>
  <% if curse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curse.errors.count, "error") %> prohibited this curse from being saved:</h2>

      <ul>
      <% curse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
